#pragma once

// Puts into string decimal representation of a number in binary form( binary notation is read from left to right)
void getPositiveInteger(const char* input, char* buffer, const int size);